% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeBMI.R
\name{computeBMI}
\alias{computeBMI}
\title{Compute Body Mass Index (BMI)}
\usage{
computeBMI(
  df,
  weight_lb = NULL,
  weight_kg = NULL,
  height_m = NULL,
  height_in = NULL,
  feet = NULL,
  inches = NULL,
  out_name = "bmi"
)
}
\arguments{
\item{df}{Data frame.}

\item{weight_lb}{Column name for weight in pounds (optional).}

\item{weight_kg}{Column name for weight in kilograms (optional).}

\item{height_m}{Column name for height in meters (optional).}

\item{height_in}{Column name for height in inches (optional).}

\item{feet}{Column name for height in feet (optional).}

\item{inches}{Column name for remaining inches (optional).}

\item{out_name}{Name of BMI column (default = "bmi").}
}
\value{
Data frame with BMI column added.
}
\description{
Adds a BMI column to the data frame.
Accepts height in feet+inches, inches only, or meters.
Weight can be in pounds ("lb") or kilograms ("kg").
}
\examples{
# Example 1: Weight in pounds, height in inches
df1 <- data.frame(
  weight_lb = c(180, 150),
  height_in = c(70, 65)
)
computeBMI(df1, weight_lb = "weight_lb", height_in = "height_in")

# Example 2: Weight in kilograms, height in meters
df2 <- data.frame(
  weight_kg = c(70, 80),
  height_m = c(1.75, 1.80)
)
computeBMI(df2, weight_kg = "weight_kg", height_m = "height_m")

# Example 3: Height in feet + inches, weight in pounds
df3 <- data.frame(
  weight_lb = c(180, 160),
  feet = c(5, 5),
  inches = c(10, 4)
)
computeBMI(df3, weight_lb = "weight_lb", feet = "feet", inches = "inches")

# Example 4: Mixed data: some rows with height in meters, some in inches
df4 <- data.frame(
  weight_kg = c(70, 80),
  height_m = c(1.75, NA),
  height_in = c(NA, 70)
)
computeBMI(df4, weight_kg = "weight_kg", height_m = "height_m", height_in = "height_in")
}
